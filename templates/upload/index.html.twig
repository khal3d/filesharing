{% extends 'base.html.twig' %}

{% block title %}Hello, World{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12 upload-container">
                <div class="alert alert-secondary" role="alert" id="upload-path" style="display: none;">
                    This is a secondary alert—check it out!
                </div>
                {{ form_start(form, {'attr': {'class': 'dropzone'}}) }}
                <div class="fallback">
                    {{ form_widget(form.file) }}
                    <input type="submit" value="Upload" />
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
